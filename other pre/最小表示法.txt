------简介------
最小表示法：把一个长为len的字符串围成一个圈，然后从任意一个字符作为起点顺时针转，都会产生一个新的长为len字符串，现在要求所有的可以产生的字符串中字典序最小的那个。



------代码------
int	Work(char st[],int len)
{
	int	i=0,j=1,k=0;
	for (;k<len;)
	if (st[(i+k)%len]==st[(j+k)%len]) ++k; else
	{
		if (st[(i+k)%len]>st[(j+k)%len]) i+=k+1; else j+=k+1;
		if (i==j) ++j;
		k=0;
	}
	return (i%len<j%len?i%len:j%len);
}



------参考------
国家集训队2003论文 周源
